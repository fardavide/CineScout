rootProject.name = "plugins"

include 'common'
include 'detekt'
include 'jvm'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from files("../gradle/libs.versions.toml")
        }
    }
}
