rootProject.name = "plugins"

include 'common'
include 'detekt'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from files("../gradle/libs.versions.toml")
        }
    }
}
