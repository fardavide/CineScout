
CREATE TABLE IF NOT EXISTS tmdbCredential (
  id INTEGER NOT NULL PRIMARY KEY,
  accessToken TEXT NOT NULL
);

insert:
  INSERT OR REPLACE
  INTO tmdbCredential(id, accessToken)
  VALUES(0, ?);

select:
  SELECT accessToken
  FROM tmdbCredential
  LIMIT 1;

delete:
  DELETE FROM tmdbCredential;
