name: Run Tests and deploy

on:
  push:
    branches:
      - 'main'

jobs:

  all-tests:
    uses: ./.github/workflows/tests.yml

  deploy:
    needs: all-tests
    uses: ./.github/workflows/deploy.yml
