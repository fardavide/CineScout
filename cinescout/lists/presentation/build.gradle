apply plugin: 'cinescout.android.compose.library'

cinescoutAndroid {
    namespace 'cinescout.lists.presentation'
}

optIns {
    androidTestExperimentalTestApi()
    experimentalCoroutinesApi()
    experimentalStdlibApi()
}

moduleDependencies {
    api 'movies:domain'
    api 'lists:domain'
    api 'tvshows:domain'

    implementation 'design'
    implementation 'utils:android'
    implementation 'utils:kotlin'

    testImplementation 'test:kotlin'
    testImplementation 'test:android'
    androidTestImplementation 'test:compose'
}

dependencies {
    ksp libs.koin.ksp

    implementation libs.androidx.lifecycle.runtime
    implementation libs.bundles.base
    implementation libs.bundles.compose

    debugImplementation libs.bundles.compose.debug 
    
    testImplementation libs.bundles.test.kotlin
    androidTestImplementation libs.bundles.test.android 
}
